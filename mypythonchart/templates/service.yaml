apiVersion: v1
kind: Service
metadata:
  name: {{ include "mypythonchart.fullname" . }}
  labels:
    {{- include "mypythonchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mypythonchart.selectorLabels" . | nindent 4 }}
